#ifndef SECRETS_H
#define SECRETS_H

// ============================================================================
// secrets.h  --  Archivo MANUAL de la fase Code (no generado).
// Copiar como `secrets.h` y completar antes del despliegue.
// No debe versionarse (ver .gitignore).
// ============================================================================

#define WIFI_SSID      "TU_SSID"
#define WIFI_PASSWORD  "TU_PASSWORD"

#define MQTT_BROKER    "192.168.1.100"   // IP del broker (p. ej., Mosquitto local)
#define MQTT_PORT      1883

#endif // SECRETS_H
